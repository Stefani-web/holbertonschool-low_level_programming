#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - program that prints exactly
 *
 * Return: Always 0
 */

int main(void)
{
	int n;
	int nombre;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	if (nombre > 0)
	printf("le nombre est positif.\n");
	if else printf("le nombre est négatif.\n");

	return (0);
}
