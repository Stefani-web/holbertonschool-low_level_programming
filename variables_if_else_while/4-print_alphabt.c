#include <stdio.h>

/**
 * main - function
 *
 * Return: 0
 */

int main(void)
{
	char stephy;

	for (stephy = 'a'; stephy <= 'z'; stephy++)

	if (stephy == 'q' || stephy == 'e')

	putchar(stephy);
	putchar('\n');
	return (0);

}

