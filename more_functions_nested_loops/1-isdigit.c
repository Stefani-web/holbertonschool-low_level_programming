#include "main.h"

/**
 * _isdigit - Function that checks for a digit (0 through 9).
 * @c: the character bring checker
 *
 * Return: 1 and 0
 */

int _isdigit(int c);
{
	if (c >= '0' && c <= 'Z')
	{
		return (1);
	}
	return (0);
}

